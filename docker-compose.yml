# services:
#   postgres:
#     image: postgres:16
#     container_name: postgres
#     environment:
#       POSTGRES_USER: ${POSTGRES_USER}
#       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#       POSTGRES_DB: ${POSTGRES_DB}
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data


#   app:
#     build:
#       context: .
#     container_name: django_app
#     depends_on:
#       - postgres
#     environment:
#       BOT_TOKEN: ${BOT_TOKEN}
#       SECRET_KEY: ${SECRET_KEY}
#       CHANNEL_ID: ${CHANNEL_ID}
#       GROUP_ID: ${GROUP_ID}
#       EXCEL_FILE: ${EXCEL_FILE}
#       Y_KASSA_TOKEN: ${Y_KASSA_TOKEN}
#     ports:
#       - "8000:8000"
#     command: >
#      sh -c "python manage.py makemigrations &&
#             python manage.py migrate &&
#             gunicorn task_manager.wsgi --bind 0.0.0.0:8000 --workers 3 --timeout 120"

# volumes:
#   postgres_data:

version: '3.8'

services:
  db:
    image: postgres:16
    container_name: postgres_db
    restart: always
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  django:
    build: .
    container_name: django_admin
    restart: always
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "8000:8000"

  bot:
    build: .
    container_name: bot
    restart: always
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - .:/app
    command: ["python", "manage.py", "runbot"]

volumes:
  postgres_data: